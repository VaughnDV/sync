# GitHub presence

Suggested repository settings (not applied remotely from this change set):

```bash
gh repo edit VaughnDV/sync \
  --description "Convert YouTube playlists into reviewable Spotify playlists through a typed Django/Celery pipeline." \
  --add-topic django --add-topic celery --add-topic spotify --add-topic youtube --add-topic openai --add-topic python
```

Social preview image: `docs/social-preview.png`. In GitHub: Settings → General → Social preview.
